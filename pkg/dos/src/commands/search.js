export const search = async () => {
  /**
   * probably will be an engine level operation, and will definitely
   * require some UI post processing to present results.
   *
   * Might allow a dedicated chain to be constructed for the search, so
   * that searching can be conducted remotely.
   *
   * Problem is dealing with streaming the results back in
   */
}
