describe('covenants', () => {
  describe('name resolution', () => {
    test.todo('refer to a covenant using a pulselink')
    test.todo('refer to a covenant using an address')
    test.todo('use a path after a bare covenant')
  })
  describe('install', () => {
    test.todo('chain reference within an installer')
    test.todo('overriding deeply nested initial state')
    test.todo('overriding a deeply nested installers children')
    test.todo('install with covenants written in multiple languages')
  })
  describe('system library', () => {
    test.todo('override system library in development')
    test.todo('run a system covenant from the published covenant')
    test.todo('system covenants are not isolated')
  })
  describe('publishing', () => {
    test.todo('urls can be used to build a covenant')
    test.todo('throw on child addition to covenant')
  })
  describe('interaction', () => {
    test.todo('remote api can be discovered')
  })
})
