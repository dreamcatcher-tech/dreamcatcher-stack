describe('dbSyncer', () => {
  test('basic', async () => {
    // load the reducer in a harness, and test it operates ?
    // load up the engine ?
    // mock the side effects, run the whole engine ?
  })
  test('edit', async () => {})
  test.todo('error')
})
