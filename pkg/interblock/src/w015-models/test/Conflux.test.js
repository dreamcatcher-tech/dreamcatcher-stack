import { Conflux } from '..'
import Debug from 'debug'
import { assert } from 'chai/index.mjs'

const debug = Debug('interblock:tests:Conflux')
Debug.enable()

describe('conflux', () => {
  test.todo('basic')
})
