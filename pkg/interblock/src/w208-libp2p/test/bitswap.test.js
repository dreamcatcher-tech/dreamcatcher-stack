describe('bitswap', () => {
  test('direct', async () => {
    // make two nodes
    // store a pulse on one of them
    // revive the pulse on the other node
  })
  test('relay', async () => {
    // make 3 nodes
    // connect them so that 1,3 is disconnected
    // store a pulse on node1
    // fully restore the pulse on node3
  })
  test('interpulse', async () => {
    // build up the interpulse piece by piece
    // show being denied for other parts of the pulse
    // get only enough to build the interpulse based on the root of the pulse
  })
})
