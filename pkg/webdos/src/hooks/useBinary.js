/**
 * Hook for gaining access and subscription to a chains binary layer.
 * Updates every time the binary changes.
 * Used for getting out images and other data.
 */

const useBinary = (path, range) => {
  //configure subscription
  // range is the byte range that you might want
}

export default useBinary
