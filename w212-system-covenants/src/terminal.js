/**
 * Terminal serves as login point for clients.
 * Initially is a single chain that handles all logins.
 * Later will only handle introductions, and will pass off clients to
 * their own dedicated chains.
 */
