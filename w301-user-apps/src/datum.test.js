const assert = require('assert')
describe('datum', () => {
  test.todo('nested datums')
  test('invalid data throws', () => {})
  test('invalid initial state throws', () => {})
  test('updates data', () => {})
  test('missing required fields throws', () => {})
})
