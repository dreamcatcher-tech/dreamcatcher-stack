describe('covenant actions', () => {
  test('non existent action fails gracefully', async () => {
    // fire up dos
    // install a custom covenant with custom actions
    // write to stdin
  })
  test('action from absolute path executes')
})
